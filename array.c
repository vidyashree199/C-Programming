//program:Counting elements in an array
#include<stdio.h>
int main(){
    int arr[]={10,20,30,40.50};
    int count=size(arr/sizeof(arr[0]));
    printf("Total number of element in the array=%d\n",count);
    return 0;
}
